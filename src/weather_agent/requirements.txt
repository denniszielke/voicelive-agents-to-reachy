# Weather Agent — outside temperature (LangGraph, Foundry hosted).
# Only the packages imported by agent.py, pinned to current releases.

# Agent server host (invocations_ws WebSocket protocol)
azure-ai-agentserver-invocations==1.0.0b6
azure-ai-agentserver-core==2.0.0b7

# LangChain / LangGraph
langgraph==1.2.8
langchain-core==1.4.9
langchain-openai==1.3.4

# Azure / runtime
azure-identity==1.25.3
azure-monitor-opentelemetry==1.8.9
httpx==0.28.1
python-dotenv==1.2.2
