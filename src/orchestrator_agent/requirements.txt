# Orchestrator Agent — routes requests to weather-agent and homeassistant-agent.
# Hosted via agent-framework-foundry-hosting (Responses protocol for VoiceLive),
# and calls its sub-agents over the invocations_ws WebSocket protocol.

agent-framework-core==1.12.1
agent-framework-foundry==1.10.3
agent-framework-foundry-hosting==1.0.0b260722

azure-identity==1.25.3
websockets==15.0.1
python-dotenv==1.2.2

# Optional: observe the agent locally in the Agent Framework DevUI
# (python -m src.orchestrator_agent.agent with DEVUI=1).
agent-framework-devui==1.0.0b260721

# Required by microsoft-opentelemetry's A365 OpenAI Agents instrumentor
# (imported transitively via agent-framework-foundry-hosting).
openai-agents==0.18.3

openai==2.47.0
